{
  "name": "tv-show-page",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "sass src/styles/sass:src/styles/css && react-scripts start",
    "build": "sass src/styles/sass:src/styles/css && react-scripts build",
    "eject": "react-scripts eject",
    "sass": "sass --watch src/styles/sass:src/styles/css"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
